{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "env": {
        "PLAYWRIGHT_HEADLESS": "true",
        "PLAYWRIGHT_ALLOWED_HOSTS": "localhost,127.0.0.1,*.local,*.test,*.dev"
      }
    }
  },
  "outputDir": ".artifacts/mcp"
}
